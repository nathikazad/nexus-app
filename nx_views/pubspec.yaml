name: nx_views
description: Shared views and HTTP data access for Nexus Flutter apps.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_map: ^8.3.0
  flutter_riverpod: ^3.2.0
  http: ^1.6.0
  intl: ^0.20.2
  latlong2: ^0.9.1
  nx_db:
    path: ../nx_db

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
