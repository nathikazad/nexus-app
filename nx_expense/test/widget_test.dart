import 'package:flutter_test/flutter_test.dart';
import 'package:nx_expense/app.dart';

void main() {
  test('NexusExpenseApp can be constructed', () {
    expect(const NexusExpenseApp(), isA<NexusExpenseApp>());
  });
}
