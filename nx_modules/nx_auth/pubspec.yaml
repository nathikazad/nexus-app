name: nx_auth
description: Shared Nexus identity, OIDC, session, and authenticated transport package.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: '>=3.11.0 <4.0.0'
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.2.0
  http: ^1.2.2
  oidc: ^3.0.1
  oidc_default_store: ^1.1.1
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4
