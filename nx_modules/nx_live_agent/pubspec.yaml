name: nx_live_agent
description: Shared realtime voice-agent session infrastructure for Nexus apps.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.12.2
  flutter: '>=3.32.0'

dependencies:
  audio_session: ^0.2.4
  flutter:
    sdk: flutter
  flutter_foreground_task: ^10.0.0
  flutter_webrtc: ^1.5.2
  http: ^1.6.0
  web_socket_channel: ^3.0.1

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
