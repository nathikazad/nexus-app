/// Embeddable Drift declarations for application-owned databases.
///
/// Most consumers should depend on `nx_offline.dart`. This separate library
/// keeps Drift out of the stable core API.
library;

export 'src/persistence/drift/offline_tables.dart';
