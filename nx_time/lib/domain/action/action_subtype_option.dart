/// One concrete Action subtype for pickers and preference seeding.
class ActionSubtypeOption {
  const ActionSubtypeOption({required this.id, required this.name});

  final int id;
  final String name;
}
